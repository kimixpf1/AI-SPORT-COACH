# 项目迭代记录

## 2026-04-01

### 本轮目标

- 修复桌面端 `ROI width and height must be > 0` 报错
- 修复微信端分析完成后的 client-side exception
- 继续把电脑端结果区调整为更均衡、更接近专业分析工作台的模块布局
- 在同步远端前先完成本地诊断、`lint`、`typecheck`、`build` 校验

### 已完成

- 为视频抽帧新增邻近时间点重试，单帧失败时自动切换到前后时间点继续识别
- 将 `seek` 后的检测改为等待可渲染帧再送入 MediaPipe，并在异常帧上直接跳过，降低 ROI 宽高为 0 的触发概率
- 历史记录读取改为结构校验与兜底清洗，避免旧版本地缓存数据在结果页渲染阶段触发异常
- 将轨迹叠加画布缩放到安全尺寸，优先降低微信端在高分辨率视频上的 canvas 内存压力
- 将电脑端结果区重构为更均衡的双列模块布局，拆分结果摘要、轨迹、节奏、姿态、建议卡片
- 本地编辑器诊断已通过，`npm run typecheck`、`npm run lint`、`npm run build` 已完成且通过

### 当前结论

- 本地代码层面已同时覆盖桌面端 ROI 问题与微信端结果渲染脆弱点
- 当前剩余重点是把最新修复同步到远端并继续直接用真实平台网址复测
- 如果后续只需增强建议文案层，可以再考虑接入 Kimi 或 DeepSeek；当前识别主链路修复不依赖额外大模型 API

## 2026-03-31

### 本轮目标

- 接续之前中断的任务
- 把 MediaPipe 本地分析链路补到可复盘状态
- 修复 GitHub Pages 部署失败与线上 404
- 增强多角度、多分辨率、低清视频下的识别兼容性
- 提升动作建议的专业程度与可执行性

### 已完成

- 补充关键姿态指标面板
- 补充动作时间线与关键时刻提示
- 扩展 `TrackingData` 结构，承载高亮节点和汇总指标
- 更新部署工作流，移除无 lockfile 时不稳定的 npm 缓存配置
- 通过 GitHub Actions API 确认 404 的直接原因是工作流构建失败，而不是网址错误
- 将 `package.json` 构建脚本改为标准 CLI 调用形式
- 调整 `next.config.ts`，使静态导出在 CI 中能继续完成
- 将 Pages 发布方式切换为构建后推送 `gh-pages` 分支
- 将 PoseLandmarker 升级为 full 模型，并加入 GPU/CPU 双模式兜底
- 将动作指标改为按可见性选择主侧或加权融合，减少侧面与遮挡场景下的误差
- 加入拍摄角度、清晰度、主体覆盖度、识别可信度评估
- 加入低质视频降级分析策略，避免因非标准角度或清晰度一般而直接无法输出结果
- 优化动作建议文案，使其更偏训练教练视角、更具体可执行
- 在页面增加识别条件提醒与拍摄评估展示
- 继续扩展侧后方与后方机位识别，补充后链与下肢对线分析
- 优化手机端上传与结果展示体验，并为上传按钮增加兜底读取策略
- 已将上传按钮灰色问题的热修复同步到远端 `main`，并触发 `gh-pages` 重新部署
- 本轮新增目标：修复微信端“选择文件”按钮失效问题，并在验证后继续推送部署
- 本轮继续新增目标：使用用户提供的真实视频复现识别失败问题，并同步优化桌面端与手机端分析界面
- 本轮继续新增目标：为 MediaPipe 运行时增加更稳定的备用资源来源，修复线上真实视频分析时的 WASM / MIME 失败
- 本轮继续新增目标：解决当前环境下 Git / GitHub CLI 推送异常，确保修复能重新部署到线上
- 本轮继续新增目标：改为直接以真实平台网址做上传与分析复测，发现问题后立刻继续热修复

### 当前结论

- Pages 地址：`https://kimixpf1.github.io/AI-SPORT-COACH/`
- Pages 站点现已可以打开
- 原先构建失败点：工作流中的 `npm run build`
- 当前修复动作：已切换为静态构建后直接发布到 `gh-pages` 分支，正在继续验证线上是否同步到最新代码
- 当前产品能力：已能对多角度、多分辨率、低清视频给出更稳健的识别结果与更专业的动作建议
- 当前线上状态：站点可访问，上传后“开始本地分析”按钮灰色的问题已完成热修复并验证可点击
- 当前新增问题：微信手机端“选择视频”按钮失效，导致上传链路被卡住，需优先修复
- 当前新增问题：真实测试视频仍可能因为有效姿态帧不足而分析失败，桌面端界面也还不够专业和对称
- 当前最新定位：线上真实视频失败不仅与姿态帧不足有关，还存在 MediaPipe CDN 运行时资源返回异常 MIME 的问题，已开始增加更稳定的运行时候选源与抽帧兜底
- 当前本地进展：已完成多轮抽帧识别兜底、运行时候选源增强和桌面端工作台式布局重构，`typecheck` 与编辑器诊断通过
- 当前阻塞点：本环境下 `git push` 与 `gh api` 推送仍未成功同步到远端，需继续排查认证或终端执行链问题
- 当前执行策略：优先绕过本地推送阻塞，把文本修复先同步到远端，再用真实平台网址和用户桌面视频做全链路复测
- 当前最新线上复测：已通过远端更新把运行时热修复推到 `main` 并触发 `gh-pages` 部署，真实平台网址下 `vision_wasm_internal.js/.wasm` 已恢复 200 响应
- 当前新增阻塞：真实平台网址下分析不再直接报清晰度错误，但会卡在“正在准备 MediaPipe 模型”，说明问题已从资源加载失败推进到运行时初始化阶段
- 当前已继续热修复：新增 head 预加载补丁，尝试更早重写 MediaPipe 运行时地址并强制切到 CPU delegate，等待线上完成新一轮部署后继续复测
- 当前用户最新反馈：电脑端已出现更明确的 MediaPipe 错误 `ROI width and height must be > 0`，说明初始化并非纯卡死，而是进入图计算后在预处理阶段拿到了无效 ROI
- 当前用户最新反馈：微信端上传视频后虽能跑满进度条，但最终触发 `Application error: a client-side exception has occurred`，需要联动排查结果渲染与移动端异常恢复
- 当前新增目标：参考原始复刻项目重整电脑端排版，改成更清晰的并排大模块或 2x2 结构，减少无意义大卡片并修正按钮断行

### 待验证

- 线上站点何时完全同步到本地最新版本
- 微信手机端上传按钮修复后是否能稳定完成选择动作、上传视频、开始分析
- 真实测试视频在优化后是否能稳定完成识别与输出专业结果
- 更完整的后方机位与手机端增强何时同步到线上
- 电脑端界面布局与交互是否达到更专业的视频分析软件观感
- 更多动作专项规则是否需要继续细化
- 本地依赖恢复后是否可以完整通过 `lint` 和 `typecheck`
- 本地运行时切换后，用户桌面测试视频是否能稳定完成上传、识别、分析与结果展示
- 远端仓库何时能成功接收本轮提交并触发 `gh-pages` 重新部署
- 运行时初始化卡住的问题是否能在新一轮 head 级热修复后彻底解除
- ROI 无效问题修复后，真实平台网址下桌面端与微信端能否都稳定完成上传、分析、结果展示和再次上传
